# Gotchas — card_games
1. **Null Values**: Promotional cards often lack standard set numbers or expansion data.
2. **Card Subtypes**: Be careful when filtering by `type`; many cards have dual types (e.g., "Creature - Artifact"). Use `LIKE` instead of `=`.
